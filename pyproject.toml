[project]
name = "nfs-heat-garagem"
version = "0.1.0"
description = "Garagem 3D para montar e customizar os veiculos extraidos do Need for Speed Heat"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
janela = ["pywebview>=5.0"]

[tool.setuptools]
packages = ["nfsgaragem"]
